streamlit==1.37.0
pandas>=2.0
numpy<2
# pyarrow is streamlit's native dataframe-serialization lib; unpinned it floats
# to brand-new releases on every redeploy. 25.0.0 (released ~July 2026) segfaults
# with streamlit 1.37 + numpy<2 on Python 3.13; <25 = the versions that ran fine.
pyarrow<25
gspread==6.1.2
gspread-dataframe==3.3.1
google-auth==2.30.0
python-dateutil>=2.8.2
watchdog==4.0.1
pyotp==2.9.0
pyyaml>=6.0
streamlit-aggrid==0.3.4.post3
plotly>=5.0
openpyxl>=3.1
